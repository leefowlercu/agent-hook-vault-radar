# HashiCorp Cloud Platform (HCP) Configuration
# Required for vault-radar CLI to function

# HCP Project ID (REQUIRED)
# Get this from your HCP Portal: https://portal.cloud.hashicorp.com/
# Navigate to your project and copy the Project ID
HCP_PROJECT_ID=

# HCP Client Credentials (for API authentication)
# Create service principal credentials in HCP Portal
# Access Control > Service Principals > Create service principal
HCP_CLIENT_ID=
HCP_CLIENT_SECRET=

# Optional: Override hook-vault-radar configuration via environment variables
# These use the HOOK_VAULT_RADAR_ prefix and map to config.yaml settings

# Logging configuration
# HOOK_VAULT_RADAR_LOGGING_LEVEL=info
# HOOK_VAULT_RADAR_LOGGING_FORMAT=json
# HOOK_VAULT_RADAR_LOGGING_LOG_FILE=~/.agent-hooks/vault-radar/logs/hook.log  # Enable file logging for debugging hooks

# Vault Radar configuration
# HOOK_VAULT_RADAR_VAULT_RADAR_COMMAND=vault-radar
# HOOK_VAULT_RADAR_VAULT_RADAR_SCAN_COMMAND=scan file
# HOOK_VAULT_RADAR_VAULT_RADAR_TIMEOUT_SECONDS=30

# Decision engine configuration
# HOOK_VAULT_RADAR_DECISION_BLOCK_ON_FINDINGS=true
# HOOK_VAULT_RADAR_DECISION_SEVERITY_THRESHOLD=medium

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your HCP credentials
# 3. The .env file is gitignored and won't be committed
# 4. For local development, you can create .env.local which overrides .env
